  <%= form_for(@note) do |f| %>
    <% if @note.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

        <ul>
        <% @note.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class='splitPane'>
      <div id='editPane' class='green-box'>
        <ul id="uedit_button_strip"></ul>
        <div class="field">
          <%= f.text_area :body, :id => 'uedit_textarea' %>
        </div>
        <div class="field">
          <%= f.text_field :title %>
        </div>
      </div>

      <div id="previewPane"></div>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
